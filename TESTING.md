# Testing
- Chạy `npm run lint` để kiểm tra TypeScript.
- Chạy `npm run build` để đảm bảo bundle production không gặp lỗi.
- E2E Testing: Kiểm tra thủ công các luồng từ Onboarding -> Mission -> X-Ray -> Share.
